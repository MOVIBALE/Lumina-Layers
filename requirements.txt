gradio>=6.0.0
numpy
opencv-python
Pillow
trimesh>=4.0.0
scipy
pystray
pytz
networkx
lxml
svglib
reportlab
colormath
svgelements>=1.9.0
shapely>=2.0.0

# PyTorch - Install separately based on your GPU:
# 
# For NVIDIA GPUs - CUDA 12.1 (Recommended for RTX 30/40 series):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# For NVIDIA GPUs - CUDA 11.8 (For older GPUs):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# For AMD GPUs - ROCm 6.2 (Linux only, for RX 6000/7000 series):
# NOTE: ROCm support is implemented but NOT tested by developers (no AMD hardware available)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/rocm6.2
#
# For CPU only (no GPU):
# pip install torch torchvision
#
# Or use start.bat (Windows) which auto-detects and installs the appropriate version
